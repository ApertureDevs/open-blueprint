apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-database
  labels:
    project: obp
    app: api-database
    layer: api
spec:
  revisionHistoryLimit: 2
  replicas: 1
  selector:
    matchLabels:
      app: api-database
  template:
    metadata:
      labels:
        project: obp
        app: api-database
        layer: api
    spec:
      containers:
        - name: api-database
          image: postgres:12.3
          imagePullPolicy: Always
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: obp-secret
                  key: api-database-password
          volumeMounts:
            - name: api-database-storage
              mountPath: /var/lib/postgresql/data
              subPath: postgres-data
          resources:
            limits:
              cpu: "1000m"
              memory: "1Gi"
            requests:
              cpu: "200m"
              memory: "128Mi"
      volumes:
        - name: api-database-storage
          persistentVolumeClaim:
            claimName: api-database-pvc
